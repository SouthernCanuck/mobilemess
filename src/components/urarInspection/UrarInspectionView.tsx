import React from 'react';
import { StyleSheet, ScrollView } from 'react-native';

const styles = StyleSheet.create({
  inspection: {
  },
});

export default (): JSX.Element => (
  <ScrollView style={styles.inspection}>
    <>
    </>
  </ScrollView>
);
