
/**
 * Maximum width of row in the form
 */
export const MAX_ROW_WIDTH = 12;

/**
 * if width is not defined for some of the field
 * form generator use that default value
 */
export const DEFAULT_FIELD_WIDTH = 12;
